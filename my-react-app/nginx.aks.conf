server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — Ingress controller handles /api/ routing to the backend service
    location / {
        try_files $uri $uri/ /index.html;
    }
}
